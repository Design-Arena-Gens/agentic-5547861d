# Lueur des Collines

Immersive narrative web experience. The production Next.js project lives in `web/` and is ready to deploy to Vercel.

## Workspace overview

- `web/` – Next.js 14 (App Router) implementation with Tailwind CSS and Framer Motion
- `.git/` – repository metadata

For usage, setup, and deployment steps, see `web/README.md`.
